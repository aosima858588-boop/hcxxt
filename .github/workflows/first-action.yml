name: 我的第一个工作流

on: [push]  # 当有 push 操作时触发

jobs:
  say-hello:
    runs-on: ubuntu-latest  # 使用 GitHub 免费提供的 Ubuntu 机器
    steps:
      - name: 打印一句话
        run: echo "Hello, GitHub Actions！我正在使用免费的 Runner！"
